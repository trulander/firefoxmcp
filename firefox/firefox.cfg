// The first line must be a comment
lockPref("xpinstall.signatures.required", false);
lockPref("extensions.autoDisableScopes", 0);
lockPref("extensions.enabledScopes", 15);

// FoxMCP WebSocket server address (read by the extension)
pref("foxmcp.websocket.url", "ws://foxmcp:8765");
